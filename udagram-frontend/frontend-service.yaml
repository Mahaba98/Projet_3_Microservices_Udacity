apiVersion: v1
kind: Service
metadata:
  labels:
    app: udagram-frontend-deployment
  name: udagram-frontend-service
  namespace: default
  selfLink: /api/v1/namespaces/default/services/udagram-frontend-deployment
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: udagram-frontend-deployment
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
